name: Publish NuGet

on:
  push:
    tags:
      - v*
  workflow_dispatch:
    inputs:
      package_version:
        description: Package version to publish. Leave empty to use the project version.
        required: false
        type: string

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Resolve package version
        id: version
        shell: bash
        run: |
          version="${{ inputs.package_version }}"
          if [ -z "$version" ] && [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            version="${GITHUB_REF_NAME#v}"
          fi
          echo "value=$version" >> "$GITHUB_OUTPUT"

      - name: Restore
        run: dotnet restore dotnet/Nona.Client.slnx

      - name: Test
        run: dotnet test dotnet/Nona.Client.slnx --configuration Release --no-restore

      - name: Pack
        shell: bash
        run: |
          if [ -n "${{ steps.version.outputs.value }}" ]; then
            dotnet pack dotnet/src/Nona.Client/Nona.Client.csproj --configuration Release --no-restore --output artifacts/nuget -p:PackageVersion=${{ steps.version.outputs.value }}
          else
            dotnet pack dotnet/src/Nona.Client/Nona.Client.csproj --configuration Release --no-restore --output artifacts/nuget
          fi

      - name: Publish
        run: dotnet nuget push artifacts/nuget/*.nupkg --api-key "${{ secrets.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate
